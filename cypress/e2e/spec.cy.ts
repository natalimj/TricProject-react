describe('Test1', () => {
  it('Join', () => {
    cy.visit("https://jolly-forest-02e0b3603-test.westeurope.1.azurestaticapps.net");
    // cy.get('[data-cy="join"]').click();
    // cy.get('[data-cy="create"]').should('be.visible');
    // cy.get('[data-cy="username"]').type("trashpanda");
    // cy.get('[data-cy="create"]').click();
    // cy.get('[data-cy="spinner"]').should('be.visible');
  })
})

export {}